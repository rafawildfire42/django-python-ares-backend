name: Deploy to EC2
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to EC2
        run: |
          ssh -i /path/to/your/private_key.pem ubuntu@18.191.144.188
          "cd /home/ubuntu/backend/ares-backend &&
          source /path/to/your/venv/bin/activate && 
          git pull && 
          poetry install &&
          python manage.py runserver